<p><%= flash[:notice] %></p>

<table width="800">
	<h1>Book detail</h1>
	<tr>
		<th></th>
		<th><%= @post_book.title %></th>
		<th><%= @post_book.body %></th>
		<% if @post_book.user == current_user %>
			<th class="edit-btn"><%= link_to "Edit", edit_post_book_path(@post_book.id), class: "pull-right" %></th>
		<% end %>

		<% if @post_book.user == current_user %>
			<th><%= link_to "Destroy", post_book_path(@post_book.id), method: :delete, "data-confirm" => "本当に削しますか？" %></th>
		<% end %>
	</tr>
</table>